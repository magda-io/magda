{
    "$schema": "http://json-schema.org/hyper-schema#",
    "title": "format relationships schema",
    "description": "a json structure describing subset, superset relations of formats, and/or relations between different versions of formats",
    "type": "object",
    "properties": {
        "childFormat": {
            "type": "object",
            "description": "The format that is the subset of the parent format(s), and/or a different version to the parent format(s)",
            "properties": {
                "parentFormats": {
                    "type": "array",
                    "description": "An array of formats that derive the child format"
                }
            }
        }
    }
}