import React from "react";

import "./Divider.scss";

/**
 * Return a divider with left and right padding, e.g abc | def
 * @returns { div } - div with character '|'
 */
const Divider = () => {
    return <div className="divider">|</div>;
};

export default Divider;
