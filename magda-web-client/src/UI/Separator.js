import React from "react";

import "./Separator.css";

/**
 * Return a separator with left and right padding, e.g abc / def. Similar to the Divider component.
 * @returns { div } - div with character '/'
 */
const Separator = () => {
    return <div className="separator">/</div>;
};

export default Separator;
