// magda-web-server creates this file on the fly, so changing
// it here will have no effect when you're using the server.
// However, this _is_ used with `npm run dev` in the client directory.
window.magda_server_config = {};
