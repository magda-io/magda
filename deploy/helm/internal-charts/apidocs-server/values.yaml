image: 
  name: "magda-apidocs-server"
  # repository: 
  # tag: 
  # pullPolicy: 
  # pullSecrets: 

defaultImage:
  repository: docker.io/data61
  pullPolicy: IfNotPresent
  pullSecrets: false

resources:
  requests:
    cpu: 10m
    memory: 10Mi
  limits:
    cpu: 20m
