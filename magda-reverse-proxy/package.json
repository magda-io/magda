{
  "name": "@magda/reverse-proxy",
  "version": "0.0.26-SNAPSHOT",
  "description": "The reverse proxy exposing MAGDA's public API.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker-build-local": "create-docker-context-for-node-component --build --push --tag auto --local",
    "docker-build-prod": "create-docker-context-for-node-component --build --push --tag auto"
  },
  "devDependencies": {
    "@magda/scripts": "^0.0.26-SNAPSHOT"
  },
  "author": "",
  "license": "Apache-2.0",
  "config": {
    "docker": {
      "name": "data61/magda-reverse-proxy",
      "include": "Dockerfile nginx.conf cors.conf"
    }
  }
}
