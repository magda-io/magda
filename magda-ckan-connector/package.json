{
  "name": "@magda/ckan-connector",
  "description": "MAGDA CKAN Connector",
  "version": "0.0.26-SNAPSHOT",
  "main": "index.js",
  "scripts": {
    "generate": "generate-registry-typescript ./src/generated/registry",
    "compile": "tsc",
    "build": "npm run generate && npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node bin/index.js",
    "startMinikube": "REGISTRY_URL=http://$(minikube ip):30010 node bin/index.js",
    "docker-build-local": "create-docker-context-for-node-component --build --tag auto --local",
    "docker-build-prod": "create-docker-context-for-node-component --build --tag auto"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@magda/registry-aspects": "^0.0.26-SNAPSHOT",
    "@magda/scripts": "^0.0.26-SNAPSHOT",
    "@types/node": "^7.0.5",
    "@types/request": "0.0.39",
    "@types/urijs": "^1.15.31",
    "typescript": "^2.1.6"
  },
  "dependencies": {
    "moment": "^2.17.1",
    "request": "^2.79.0",
    "ts-node": "^2.1.0",
    "urijs": "^1.18.7"
  },
  "config": {
    "docker": {
      "name": "data61/magda-ckan-connector",
      "include": "aspect-templates bin Dockerfile node_modules"
    }
  }
}
